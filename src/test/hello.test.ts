import t = require('tap');
//t.runOnly = true;

//================================================================================

t.test('hello world', async (t: any) => {
    t.ok(true, 'hello world');
    t.done();
});
